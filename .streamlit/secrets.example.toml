# Streamlit Secrets Template
# This file is NOT committed to GitHub for security
# On Streamlit Cloud, add these secrets via the Settings → Secrets panel

# Groq API Key - Required for LLM
GROQ_API_KEY = "your_groq_api_key_here"

# Optional: Anthropic API Key (if using Claude instead)
# ANTHROPIC_API_KEY = "your_anthropic_key_here"

# Optional: HuggingFace API Key (for embeddings if needed)
# HUGGINGFACE_API_KEY = "your_huggingface_key_here"
